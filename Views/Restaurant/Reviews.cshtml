@foreach(var review in @ViewBag.Reviews)
{
    <h3>Review for : @review.Restaurant.Name <span class="star"> @review.Star Stars</span></h3>
    <h4>Reviewer : @review.UserName - @review.Date.ToString("MMM dd, yyyy")</h4>
    <h4 class="review">@review.Content</h4>
    <hr>
}